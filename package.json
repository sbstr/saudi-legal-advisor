{
  "name": "saudi-legal-advisor",
  "version": "0.1.0",
  "private": true,
  "description": "Arabic legal-advisor chat for Saudi law, backed by the Anthropic API.",
  "main": "backend/server.js",
  "scripts": {
    "start": "node --env-file=.env backend/server.js",
    "dev": "node --env-file=.env backend/server.js"
  },
  "engines": {
    "node": ">=20.6.0"
  },
  "devDependencies": {
    "pdf-parse": "^2.4.5"
  }
}
